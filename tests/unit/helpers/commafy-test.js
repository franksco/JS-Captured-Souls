import { commafy } from 'captured-souls/helpers/commafy';
import { module, test } from 'qunit';

module('Unit | Helper | commafy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commafy([42]);
  assert.ok(result);
});
